"""Small deterministic corpus used by smoke and baseline runs."""

EXAMPLE_TEXTS = [
    "A GPU room joins independent providers for useful scientific computing.",
    "Adapter training updates a small set of parameters while the base model stays frozen.",
    "Reproducible experiments record seeds, software versions, hardware, and metrics.",
    "Single-node training has zero synchronization traffic between workers.",
    "Checksums protect binary model updates from accidental corruption.",
    "Checkpoint metadata makes interrupted training safe to resume.",
    "Gradient accumulation enables larger effective batches on limited memory.",
    "Quantized base weights reduce memory while adapters remain trainable.",
]
