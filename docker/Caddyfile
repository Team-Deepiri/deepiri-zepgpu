{$COORDINATOR_DOMAIN:localhost} {
    encode zstd gzip

    handle /api/* {
        reverse_proxy api:8000
    }

    handle /metrics {
        reverse_proxy api:8000
    }

    handle {
        reverse_proxy ui:80
    }
}
